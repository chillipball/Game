/** The four states of the Jarvis orb animation */
export type OrbState = "idle" | "listening" | "thinking" | "talking";

/** Assistant connection state */
export type ConnectionState = "disconnected" | "connecting" | "connected";
