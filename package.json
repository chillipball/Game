{
  "name": "jarvis-root",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r --parallel run build",
    "start:server": "pnpm --filter server start",
    "dev:client": "pnpm --filter client dev",
    "bot:paper": "pnpm --filter @jarvis/bot run paper",
    "bot:live": "pnpm --filter @jarvis/bot run live"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
